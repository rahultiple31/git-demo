version: 0.0
os: linux
files:
  - source: /
    destination: /home/ubuntu
hooks:
  AfterInstall:
    - location: /etc/httpd/config/httpd.conf
      timeout: 100
  ApplicationStart:
    - location: /var/www/html/index.html
      timeout: 100
